services:
  file-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: file-server
    env_file:
      - .env
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"   # 上传 API 端口
    volumes:
      - ./Files:/usr/share/nginx/html/files
      # 证书目录需含 fullchain.pem + privkey.pem（LE SAN 证书，与酒馆共用）
      - ${AVG_CERT_DIR:-./SSL}:/etc/nginx/ssl:ro
    environment:
      - PORT=8080
      - UPLOAD_DIR=/usr/share/nginx/html/files
      - FILE_SERVER_BASE_URL=/files
    restart: unless-stopped
    networks:
      - file-server-network

networks:
  file-server-network:
    driver: bridge
